{# templates/home.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Your lucky number is hello</h1>
    <ul>
    {% for race in races %}
        <li><a href="{{ path('race',{'id':race.id}) }}">{{ race.name }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}