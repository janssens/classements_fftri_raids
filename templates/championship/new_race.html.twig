{% extends 'base.html.twig' %}

{% block title %}New Race{% endblock %}

{% block body %}
    <h1>Nouvelle course pour cette saison</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Valider') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('race_index') }}">Voir toutes les courses</a>
{% endblock %}